import {AppRegistry} from 'react-native';
import ReactNativeWorkmateApp from './src';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => ReactNativeWorkmateApp);
